# 第2章 多元回归与特征工程

## 目标

从单特征扩展到多特征系统。

---

## 必学内容

### 多元线性回归

```python
f(x) = w₁x₁ + w₂x₂ + ... + wₙxₙ + b
```

向量化形式：

```python
f(x) = w·x + b
```

### 向量化计算

```python
# 不用 for 循环
np.dot(w, x) + b
```

### 特征缩放

```python
# 均值归一化 / Z-score
x_scaled = (x - μ) / σ
```

为什么需要：

加速梯度下降收敛。

### 特征工程

- 多项式特征
- 交互特征

---

## 必须真正理解

### 为什么向量化更快

### 特征缩放的本质

让损失函数的等高线更接近圆形，梯度下降路径更直。

---

## AI联系

真实场景中：

- 房价预测 = 多特征回归
- 任何表格数据预测 = 多元回归基础

---

