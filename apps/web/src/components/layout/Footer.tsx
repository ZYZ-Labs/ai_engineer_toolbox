export function Footer() {
  return (
    <footer className="border-t border-line bg-white">
      <div className="mx-auto flex max-w-7xl flex-col gap-3 px-4 py-8 text-sm text-muted sm:px-6 lg:px-8">
        <p>AI Engineer Toolbox is open source, local-first, and intentionally backend-free for v1.</p>
        <p>All processing happens locally in your browser whenever possible. Your data is not uploaded unless explicitly stated.</p>
      </div>
    </footer>
  );
}
